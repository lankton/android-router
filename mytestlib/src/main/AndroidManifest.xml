<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="cn.lankton.mytestlib">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".LibOneActivity" />
        <activity android:name=".LibTwoActivity" />
    </application>

</manifest>
